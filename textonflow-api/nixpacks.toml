[phases.setup]
aptPkgs = ["fonts-liberation", "fontconfig", "fonts-noto-color-emoji", "fonts-noto-extra"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "fc-cache -fv",
  "mkdir -p static/temp"
]
